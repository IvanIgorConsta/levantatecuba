# ============================================================================
# VARIABLES DE ENTORNO FRONTEND - LEVANTATECUBA
# ============================================================================
# Este archivo documenta TODAS las variables de entorno usadas en el frontend
# Copiar a .env en la raíz del proyecto y configurar según el entorno
# ============================================================================

# ============================================================================
# DESARROLLO (npm run dev)
# ============================================================================
# En desarrollo, Vite usa proxy para /api, no necesitas estas variables
# Solo configúralas si quieres probar contra un servidor remoto

# API Base URL (opcional en desarrollo, necesario en producción)
# Dejar vacío usa el proxy de Vite: /api → http://localhost:5000/api
# VITE_API_BASE_URL=

# ============================================================================
# PRODUCCIÓN
# ============================================================================
# En producción, el frontend se sirve desde el mismo dominio que el backend
# Por lo tanto, las rutas relativas (/api) funcionan automáticamente
# Solo configurar si el backend está en un dominio diferente

# VITE_API_BASE_URL=https://levantatecuba.com

# ============================================================================
# TIENDA (Shopify Integration)
# ============================================================================
# Modo de la tienda: 'internal' (catálogo propio) o 'external' (redirect)
VITE_STORE_MODE=internal

# URL externa de la tienda (solo si STORE_MODE=external)
# VITE_STORE_EXTERNAL_URL=https://shop.levantatecuba.com

# Shopify Storefront API (solo si usas Shopify)
# VITE_USE_SHOPIFY=true
# VITE_SHOPIFY_STORE_DOMAIN=tu-tienda.myshopify.com
# VITE_SHOPIFY_STOREFRONT_TOKEN=tu_token_storefront_api
# VITE_SHOPIFY_API_BASE=/api/shopify

# ============================================================================
# CONFIGURACIÓN OPCIONAL
# ============================================================================
# URL de la tienda interna (por defecto usa URL del sitio)
# VITE_STORE_URL=

# Debug de ShopCTA (solo desarrollo)
# VITE_DEBUG_SHOP_CTA=true

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
# 1. Las variables DEBEN empezar con "VITE_" para ser expuestas al frontend
# 2. NO incluir secretos sensibles (API keys privadas, secrets, etc.)
# 3. En producción, las variables se inyectan en tiempo de BUILD
# 4. Después de cambiar .env, REINICIAR el servidor: npm run dev
# 5. Variables sin prefijo VITE_ NO estarán disponibles en el frontend

# ============================================================================
# PRECEDENCIA DE CONFIGURACIÓN
# ============================================================================
# Frontend usa la siguiente precedencia:
# 1. Variables de entorno (.env) - PRIORIDAD MÁXIMA
# 2. Archivo public/app-config.json - Fallback
# 3. Valores por defecto en código
#
# Recomendación: Usar .env en desarrollo, app-config.json en producción
# ============================================================================
